{
  "private": true,
  "name": "split",
  "scripts": {
    "start": "yarn workspace frontend start",
    "build": "yarn workspace frontend build:web",
    "format": "yarn workspace frontend format && yarn workspace functions format",
    "lint": "yarn workspace frontend lint && yarn workspace functions lint",
    "deploy:web": "yarn build && firebase deploy --only hosting",
    "deploy:functions": "firebase deploy --only functions"
  },
  "workspaces": [
    "functions",
    "frontend",
    "shared"
  ]
}
