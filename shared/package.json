{
  "name": "shared",
  "types": "src/index.ts",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "format": "prettier --list-different --write src/**/*.ts",
    "check-types": "tsc --noEmit",
    "check": "yarn check-types"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  },
  "dependencies": {
    "typescript": "^5.7.2"
  }
}
